allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
 
buildscript {
    repositories {
        google() // Add this line
        mavenCentral() // Add this line
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.2.2' // Update if needed
        classpath 'com.google.gms:google-services:4.3.15' // Ensure this is included
    }
}

allprojects {
    repositories {
        google() // Add this line
        mavenCentral() // Add this line
    }
}
